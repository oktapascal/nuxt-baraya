export interface ProfileUserResume {
    nik: string
    foto: string
}

export interface ProfileUserComplete {
    nik: string|null
    foto: string|null
    nama: string|null
    alamat: string|null
    no_telp: string|null
    email: string|null
}